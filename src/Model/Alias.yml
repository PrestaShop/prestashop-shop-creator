fields:
  class: 'Alias'
  columns:
    alias:
      type: word
      unique: true
    search:
      type: text
      args:
        - 64
    active:
      type: boolean
      args:
        - 95 # probability of getting a true for this param
