fields:
    class: 'AttributeGroup'
    columns:
        id:
          type: increment
        is_color_group:
            type: boolean
            args:
                - 95 # probability of getting a true for this param
        group_type:
          type: randomElement
          args:
            -
              - select
              - radio
              - color
fields_lang:
    columns:
        name:
          type: words
          args:
            - 2 #nb words
        public_name:
          type: words
          args:
            - 2 #nb words
