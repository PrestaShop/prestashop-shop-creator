fields:
    primary: 'id_carrier, id_group'
    sql: 'a.id_carrier > 1'
    columns:
#        id:
#          type: increment
        id_carrier:
          relation: Carrier
        id_group:
          relation: Group
