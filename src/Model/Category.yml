fields:
    id: 'name'
    class: 'Category'
    image: 'c'
    sql: 'a.id_category > 2'
    columns:
        name:
          type: word
          args:
            - 10 #nb chars
          skip: true
        id_parent:
          relation: Category
        active:
          type: boolean
          args:
            - 90 # probability of getting a true for this param
        is_root_category:
          value: 0
fields_lang:
    id_shop: 1
    columns:
        description:
          type: text
        link_rewrite:
          type: word
          args:
            - 10
entities:
    Home:
      fields:
        id_parent: Root
        active: 1
        is_root_category: true
      skip: true
