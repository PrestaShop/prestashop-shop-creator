fields:
    primary: 'id_category, id_group'
    sql: 'a.id_category > 1'
    columns:
        id:
          type: md5
          unique: true
        id_category:
          relation: Category
        id_group:
          relation: Group