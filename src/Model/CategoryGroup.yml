fields:
    primary: 'id_category, id_group'
    columns:
        id:
          type: md5
          unique: true
        id_category:
          relation: Category
        id_group:
          relation: Group