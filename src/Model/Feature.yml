fields:
    class: 'Feature'
    columns:
        id:
          type: increment
        position:
          type: numberBetween
          args:
           - 1
           - 100
fields_lang:
    columns:
        name:
          type: word
          unique: true