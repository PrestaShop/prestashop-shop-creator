fields:
    class: 'FeatureValue'
    columns:
        id:
          type: increment
        id_feature:
          relation: Feature
        custom:
          type: boolean
          args:
            - 50
fields_lang:
    columns:
        value:
          type: word
          unique: true