fields:
    class: 'FeatureValue'
    columns:
        id:
          type: increment
        id_feature:
          relation: Feature
          generate_all: true
        custom:
          type: boolean
          args:
            - 50
fields_lang:
    columns:
        value:
          type: word