fields:
    id: 'value'
    class: 'FeatureValue'
    columns:
        id:
          type: increment
        id_feature:
          relation: Feature
          generate_all: true
        custom:
          type: boolean
          args:
            - 50
        #needed for id, but not useful
        # todo remove this ? 
        value:
            type: word
fields_lang:
    columns:
        value:
          type: word
