fields:
    class: 'OrderHistory'
    columns:
        id_employee:
          type: numberBetween
          args:
            - 0
            - 1
        id_order:
          relation: Order
          generate_all: true
        id_order_state:
          relation: OrderState
