fields:
    id: 'iso_code'
    columns:
        id_country:
            relation: Country
        id_zone:
            relation: Zone
        name:
            type: word
        iso_code:
            type: word
        tax_behavior:
            type: boolean
        active:
            type: boolean
